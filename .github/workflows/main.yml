name: Deploy to FTP

on:
  push:
    branches:
      - main  # Trigger only when pushing to the main branch

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.nahushlogistic.com
          username: sagar@nahushlogistic.com
          password: narQum-qisbaq-qodvi4
          server-dir: ./public_html/  # The path on your server
